---
description: (alias) Analyze a multi-step flow — now /pom-analyze-flow
argument-hint: <route.yml | inline description>
---

This command has been renamed. Tell the user, then run `/pom-analyze-flow $ARGUMENTS`.

It analyzes each page in the flow and writes one artifact per page; `/pom-generate <slug>` then
writes the Page Objects for each. No code is generated by the analysis step.
